# Roadmap — пріоритети розвитку Matadora

Цей документ синтезує дослідницькі звіти (`docs/research/*.md`) в один порядок дій.
Агент-Розробник орієнтується на секцію "Технічні завдання" зверху вниз, по одному
завданню за раз.

**Ревізія:** 2026-07-03

## 🥇 Пріоритет №0 — швидкий кешфлоу (терміново)

**Переналаштувати `/kosztorys` під нову аудиторію: архітектори, управителі
нерухомості, дизайнери інтер'єру** — не тільки генеральні підрядники.

Обґрунтування (дослідження "willingness-to-pay" + "DFY vs SaaS"): рушій
"PDF→кошторис" уже готовий і оплачений через Stripe. Найшвидший шлях до
першого доходу — не будувати нове, а продавати той самий інструмент іншій
аудиторії напряму (DFY-послуга за документ), а не чекати самообслуговування.

Технічно потрібно (для агента-Розробника):
- Оновити копірайтинг на `/kosztorys` — прибрати мову, орієнтовану виключно
  на "generalny wykonawca", додати мову для архітекторів/управителів
  нерухомості/дизайнерів (напр. "оцінка вартості ремонту для клієнта" замість
  тільки "kosztorys budowlany")
- Перевірити, чи приклади/дефолтні KNR-позиції залишаються релевантними для
  цієї аудиторії (вони достатньо загальні — ремонт/оздоблення, мають підійти)
- НЕ чіпати сам розрахунковий рушій (`computeOfferTotals`, KNR-каталог,
  Stripe-інтеграцію) — тільки позиціонування/копірайт і, за потреби, нову
  секцію на лендінгу "для кого цей інструмент"
- Малий, безпечний обсяг змін — тільки текст/UI, без зміни схеми БД

Продаж і клієнтський аутріч — це дія власника, не агента (див. RECOVERY.md
принципи меж автоматизації).

## ✅ Виконано

- ~~Валідація кошторису перед відправкою~~ — змерджено (PR #7, 2026-07-04).

## 🥈 Пріоритет №1 — зробити кошторис привабливішим (більше інтересу/користувачів)

Власник прямо підтвердив: мета зараз — залучити **більше** людей до
`/kosztorys`, не звужувати аудиторію. Ці фічі роблять сам інструмент
кращим і привабливішим, а не додають нові модулі.

1. **Ієрархічне групування позицій кошторису** (секціями: ванна→демонтаж→
   плитка, з підсумком по секції) — зараз плаский список, важко читати
   великі кошториси. **Міграція вже застосована** (0051_offer_stage_grouping.sql,
   `offer_stages.group_label` — nullable text, backward compatible) — власник
   зробив це вручну, бо агенту заборонено чіпати `supabase/migrations/`.
   Залишається тільки: UI для присвоєння секції позиції, згортання/розгортання
   груп, підсумок по секції в `kosztorys/page.tsx` та `offer-wizard.tsx`.
   НЕ потрібна нова міграція для цього пункту.
   Джерело: `openconstructionerp-analysis.md` §2.1.

2. **Розширений каталог позицій із пошуком** — зараз `KNR_ITEMS` це
   захардкоджений масив із 12 записів. Перенести в таблицю `cost_items` у
   Supabase + повнотекстовий пошук (`tsvector`) замість підрядкового.
   Складність: мала (full-text) — велика (якщо додавати семантичний пошук).
   Джерело: `openconstructionerp-analysis.md` §2.2.

3. **Версіонування цін по кварталах/регіонах** — зараз статичний напис
   "orientacyjne, region centralny, Q1 2026" прямо в коді. Залежить від
   таблиці `cost_items` з п.2 — додає `region`/`quarter` колонки і просту
   адмін-форму оновлення без деплою коду.
   Джерело: `openconstructionerp-analysis.md` §2.6.

## Технічні завдання (інші модулі, нижчий пріоритет зараз)

4. **EVM S-curve графік** — дані вже є в `evm_snapshots`
   (`bac`/`pv`/`ev`/`ac`/`cpi`/`spi`), просто немає жодної бібліотеки графіків
   у всьому коді. Чиста фронтенд-задача на `evm-client.tsx`.
   Джерело: `platform-wide-feature-analysis.md` §4.1.

5. **Картка "готівка зараз + прогноз на 4-8 тижнів"** — агрегація
   `cashflow_entries` + `invoices` + `retention_payments` в одну картку на
   `/dashboard/contractor/finanse`.
   Джерело: `platform-wide-feature-analysis.md` §4.2.

6. **Бейдж кваліфікації підрядника біля ставки** — прострочена страховка/
   ліцензія видно одразу в списку ставок, дані вже є, тільки читання.
   Джерело: `platform-wide-feature-analysis.md` §1.3.

7. **Матриця порівняння ставок side-by-side** — замість двох окремо
   відсортованих списків.
   Джерело: `platform-wide-feature-analysis.md` §1.1.

8. **Історія оцінок підрядника по проєктах** — нова таблиця
   `subcontractor_reviews` замість однієї перезаписуваної `rating`.
   Джерело: `platform-wide-feature-analysis.md` §3.1.

## Стратегічний трек — НЕ для агентів, потребує людини

1. **Партнерство з факторинговою компанією** (Bibby Financial Services,
   INDOS, PKO Faktoring) — реферальна комісія 10-20%. Дзвінок/лист власника.
2. **DFY-продажі `/kosztorys` новій аудиторії** — перші 5-10 повідомлень
   архітекторам/управителям нерухомості з наявної мережі контактів.
3. **Розмова з księgowy** про легалізацію діяльності — передумова для
   факторингу й будь-якого зовнішнього фінансування.

## Свідомо відкладено

- KSeF live-submission API — власник прямо вирішив не інвестувати сюди понад
  обов'язковий мінімум. **Не пропонувати як пріоритет.**
- Offline-режим для роботи на об'єкті — велика робота, немає ще запиту.
- Суперечки/претензії, гарантії/страхування — дослідження прямо каже "докази
  слабкі, не пріоритет".
- Маркетплейс матеріалів, cost-index як окремий продукт — потребують
  масштабу транзакцій, якого ще немає.
- Загальний "AI wrapper" SaaS з нуля — дослідження показало це як
  найпереоціненіший, найменш надійний шлях до доходу.

## Що ще не відомо

- Поточна кількість реальних платних клієнтів і unit-економіка pay-per-use
  kosztorys — внутрішні дані, не публічна інформація.

## Бэклог ідей розвитку (сесія 2026-07-19) — не пріоритизовано, на майбутнє

Власник попросив зафіксувати список для подальшого розбудовування. **Не
починати без явного пріоритезування власником** — частина пунктів прямо
суперечить рішенням вище ("Свідомо відкладено" / "Стратегічний трек") і
позначена окремо.

**AI-монетизація (той самий принцип pay-per-use, що й Adwokat AI/BHP-фото)**
- AI-перевірка кошторису на заниження/завищення цін відносно ринкових даних
- AI-аналіз дозволу на будівництво (pozwolenie na budowę) на повноту
- AI-оцінка вартості ремонту за фото приміщення "до робіт"
- AI-асистент заповнення заявок/зголошень робіт до нагляду будівельного
- AI-порівняння кількох версій кошторису з поясненням відмінностей

**Комплаєнс і документообіг**
- ~~Реальна інтеграція з KSeF (подання через API MF)~~ — **вже відхилено
  власником**, див. "Свідомо відкладено" вище. Не пропонувати повторно без
  нового явного запиту.
- Автонагадування про закінчення строків дозволів/атестацій/BHP-документів
- AI-генератор протоколу передачі майданчика (przekazanie placu budowy)
- Автоперевірка позицій кошторису на відповідність нормам KNR

**Фінанси**
- ~~Факторинг для інвесторів~~ — вже в "Стратегічний трек" вище (п.1,
  потребує дії власника, не агента)
- Автогенерація довідок для PIT/CIT для одноосібних підприємців
- ML-прогноз cash flow на основі історії проєктів (розширення наявного
  Cash Flow модуля)
- Open Banking (PSD2) — автозвірка банківських платежів із рахунками

**Маркетплейс і ріст**
- Реферальна програма — знижка на AI-функції за запрошеного підрядника
- SEO-оптимізовані публічні профілі підрядників (розширити наявний `/firm/[id]`)
- ~~Маркетплейс матеріалів з автозамовленням~~ — вже позначено як
  "потребує масштабу, якого ще немає" у "Свідомо відкладено"
- Партнерство зі страховими — страхування об'єкта/відповідальності

**Аналітика**
- Анонімний бенчмаркінг цін по регіону на основі завантажених кошторисів
- Дашборд "здоров'я бізнесу" підрядника (маржа, backlog, запас коштів)
- Прогноз ризику затримки проєкту на основі темпу виконання

**Кадри**
- AI-скринінг кваліфікацій/резюме робітників
- Табель через геолокацію + автоматичний розрахунок зарплати (розширення
  наявного geolocation-модуля)
- Автоматичний розрахунок внесків ZUS для бригад

**Довіра і безпека угод**
- Ескроу-платежі — кошти інвестора блокуються до підтвердження етапу
  (перетинається зі старою "ФАЗА 12" у кореневому `ROADMAP.md`, там не
  реалізовано)
- Кваліфікований електронний підпис документів (замість простого e-signature)
- Автоверифікація підрядника через API CEIDG/KRS замість ручної

**UX і мобільність**
- PWA/мобільний застосунок — фото з майданчика напряму в галерею проєкту
- ~~Офлайн-режим для BHP-чек-листів~~ — вже позначено як "велика робота,
  немає ще запиту" у "Свідомо відкладено"
- Push-сповіщення про статус тендерів, платежів, дедлайнів

**Розширення AI-екосистеми**
- ~~Adwokat AI: медіація типової суперечки~~ — перетинається з "Суперечки/
  претензії... докази слабкі, не пріоритет" у "Свідомо відкладено"
- Kosztorys AI: пакетний аналіз кількох PDF одразу (для великих тендерів)
- BHP AI: аналіз відео (не лише фото) з обходу майданчика

**Масштабування**
- Локалізація під інший ринок (Німеччина/Україна) з адаптованим
  законодавством і валютою
